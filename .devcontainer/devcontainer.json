{
  "name": "GENEALOGIX Spec",
  "image": "mcr.microsoft.com/devcontainers/go:1.26",
  "features": {
    "ghcr.io/devcontainers/features/node:1": {
      "version": "24"
    },
    "ghcr.io/devcontainers/features/github-cli:1": {}
  },
  "postCreateCommand": {
    "go-deps": "go mod download",
    "go-tools": "set -e; v=\"$(tr -d '[:space:]' < .golangci-lint-version)\"; printf '%s' \"$v\" | grep -qE '^v[0-9]+\\.[0-9]+\\.[0-9]+$' || { echo \"invalid .golangci-lint-version: $v (expected vMAJOR.MINOR.PATCH)\" >&2; exit 1; }; tmpfile=$(mktemp); curl -sSfL \"https://raw.githubusercontent.com/golangci/golangci-lint/${v}/install.sh\" -o \"$tmpfile\" && sh \"$tmpfile\" -b \"$(go env GOPATH)/bin\" \"$v\"; rm -f \"$tmpfile\"",
    "install-hooks": "make install-hooks",
    "spec-deps": "cd specification && npm ci",
    "website-deps": "cd website && npm ci"
  },
  "forwardPorts": [5173],
  "customizations": {
    "vscode": {
      "settings": {
        "go.toolsManagement.autoUpdate": true,
        "go.useLanguageServer": true,
        "eslint.workingDirectories": ["website"],
        "editor.formatOnSave": true,
        "[go]": { "editor.defaultFormatter": "golang.Go" },
        "[vue]": { "editor.defaultFormatter": "esbenp.prettier-vscode" },
        "[json]": { "editor.defaultFormatter": "esbenp.prettier-vscode" },
        "[jsonc]": { "editor.defaultFormatter": "esbenp.prettier-vscode" },
        "[markdown]": { "editor.defaultFormatter": "esbenp.prettier-vscode" }
      },
      "extensions": [
        "golang.Go",
        "esbenp.prettier-vscode",
        "dbaeumer.vscode-eslint",
        "github.vscode-github-actions",
        "anthropic.claude-code",
        "Vue.volar",
        "redhat.vscode-yaml",
        "DavidAnson.vscode-markdownlint"
      ]
    }
  },
  "remoteUser": "vscode"
}
