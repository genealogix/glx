{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/genealogix/glx/main/specification/schema/v1/vocabularies/relationship-types.schema.json",
  "title": "Relationship Types Vocabulary",
  "description": "Defines the standard and custom relationship types available for relationship entities",
  "type": "object",
  "required": ["relationship_types"],
  "properties": {
    "relationship_types": {
      "type": "object",
      "description": "Map of relationship type definitions",
      "additionalProperties": {
        "$ref": "#/definitions/RelationshipTypeDefinition"
      }
    }
  },
  "additionalProperties": false,
  "definitions": {
    "RelationshipTypeDefinition": {
      "type": "object",
      "description": "Definition of a single relationship type",
      "required": ["label"],
      "properties": {
        "label": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable label for the relationship type"
        },
        "description": {
          "type": "string",
          "description": "Detailed description of the relationship type"
        },
        "gedcom": {
          "type": "string",
          "description": "Corresponding GEDCOM tag for import/export mapping"
        }
      }
    }
  }
}
